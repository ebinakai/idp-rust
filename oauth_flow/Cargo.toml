[package]
name = "oauth_flow"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.22.1"
chrono = { version = "0.4.44", features = ["serde"] }
crypto = { version = "0.1.0", path = "../crypto" }
rand = "0.10.1"
uuid = { version = "1.23.1", features = ["v4"] }
